global:
  scrape_interval: 15s

scrape_configs:
  - job_name: llm-gateway
    metrics_path: /metrics
    scrape_interval: 5s
    authorization:
      credentials: dev-metrics-token
    static_configs:
      - targets:
          - backend:8000
